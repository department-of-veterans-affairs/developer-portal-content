We updated the supporting documents returned in V1 GET /services/claims/v1/claims and V1 /services/claims/v1/forms/526/{claim-id}/attachments to display the filename, md5, header_hash, and uploaded_at values on the supporting document(s) correctly.