We updated our v2 GET /veterans/{veteranId}/claims/{id} endpoint to include the 'uploadDateTime' attribute.

Supporting documents returned in the response now include the upload date and time. These timestamps use the ISO 8601 format and are expressed in UTC.
